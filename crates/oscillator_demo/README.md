# oscillator_demo

A small pedagogical harmonic-oscillator library for Grass. It provides one
solver's state, semi-implicit-Euler integrator, declarative plugin config,
and a typed position-port payload. Applications own instance count, scheduling,
and coupling policy. The runnable composition and analytical validation live in
[`examples/oscillator_demo`](../../examples/oscillator_demo/README.md).
