//! See `sweep.py` and the grass_app unit tests for this lifecycle validation.
//!
//! This example intentionally keeps its checks in the reusable app API tests;
//! the accompanying validation script records their pass/fail matrix.

fn main() {
    println!("Run ../../automation/bin/run-bench.sh examples/fallible_lifecycle");
}
